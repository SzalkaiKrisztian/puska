# Ide várom a függvény szignatúrákat
# --------------------------------------------------------------------------------------------

# generateTableCell(cellType: "td" | "th", tartalom: string, parentTr: HTMLTableRowElement): HTMLTableCellElement

# generateFrame(sectionId: string, tbodyId: string, fejlec: string[]): void

# generateTableRow(parentBodyId: HTMLTableSectionElement, obj: TableRowObj): void

# generateTableBody(arr: TableRowObj[]): void

# hideIfChecked(negyzet: HTMLInputElement): void

# valtozas(e: Event): void

# -----------------------1. commit--------------------------utolag, sajna kifelejtettem az elsonél

# bR(parentDiv: HTMLDivElement): void

# generateFormDiv(parentForm: HTMLFormElement, labinpId: string, labContent: string): void

# generateForm(formId: string, arrForm: jsFormObj[]): HTMLFormElement

# ------------------------2. commit------------------------------------

# generateTableRowCol(parentBodyId: HTMLTableSectionElement, obj: TableRowObj): void

# validField(inputInput: HTMLInputElement, errorTxt: string): boolean

# validateFields(telepInput: HTMLInputElement, agazatInput: HTMLInputElement, peldaInput: HTMLInputElement): boolean

# addToHtmlTable(e: Event): void

# -------------------------3. commit---------------------